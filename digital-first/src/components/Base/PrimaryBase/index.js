import React from 'react'
import '../style.css'

function PrimaryBase( {children}) {
    return (
        <div className='primary-base-container'>
            {children}
        </div>
    )
}

export default PrimaryBase;